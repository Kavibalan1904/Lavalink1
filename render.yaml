services:
  - type: web
    name: reso-lavalink
    env: docker
    plan: free
    region: singapore
    dockerfilePath: ./Dockerfile
    healthCheckPath: /v4/info
    envVars:
      - key: PORT
        value: "8080"
